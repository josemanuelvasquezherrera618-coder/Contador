/*function funcionConParametros(name) {
    // Estas son comillas invertidas (backticks) ` 
    console.log(`Hola ${name}`); 
}

funcionConParametros("Jose");

function saludoCompleto(nombre, apellido) {
    console.log(`Hola ${nombre} ${apellido}`);
}

saludoCompleto("Jose", "Vásquez");

funcionConParametros("Maria"); {
saludoCompleto("Maria", "Gonzalez");
}


function suma(a, b) {
    return a + b;
}


let resultado = suma(5, 3);
console.log(resultado);

*/

/*Promedio de notas

Crea un afunción que reciba un array de notas.
Usa for para sumarlas
Devuelve el promedio.
Di si aprueba (>=3.0) o no 

function mostrar(){
    return "Hola";
}

let dato = mostrar;
console.log(dato());
*/
/*
function cuadrado(num) {
    return num * num;
}

let r = cuadrado(2);
console.log(r)

let suma = r + 10;
console.log(suma);

function suma(a, b) {
    return a + b;
}
let resultado = suma(2, 3);
console.log(resultado);

let resultado2 = suma(resultado, 10);
console.log(resultado2);

function promedioNotas(notas) {
    let suma = 0;
    for (let i = 0; i < notas.length; i++) {
        suma += notas[i];
    }
    let promedio = suma / notas.length;
    console.log(`El promedio es: ${promedio}`);
    if (promedio >= 3.0) {
        console.log("¡Aprobado!");
    } else {
        console.log("Reprobado");
    }
}

let notasAlumno = [4.0, 3.5, 2.0, 5.0];
promedioNotas(notasAlumno);

*/